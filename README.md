# Teatro Estrella - Aplicación SaaS de Venta de Tickets

Aplicación web profesional para la venta de tickets de teatro desarrollada con Next.js, TypeScript y Supabase.

## Características

- ✅ **Autenticación de usuarios** con validación de contraseña (10+ caracteres, símbolo especial)
- ✅ **Página de inicio** con hero banner, funciones destacadas e información del teatro
- ✅ **Listado de funciones** con ordenamiento alfabético y por precio
- ✅ **Búsqueda en tiempo real** de funciones de teatro
- ✅ **Formulario de contacto** con validación y guardado en base de datos
- ✅ **Diseño responsive** optimizado para móvil, tablet y desktop
- ✅ **Animaciones y transiciones** modernas

## Stack Tecnológico

- **Framework:** Next.js 14 (App Router)
- **Lenguaje:** TypeScript
- **Estilo:** CSS Modules + CSS Variables
- **Base de datos:** Supabase (PostgreSQL)
- **Autenticación:** Supabase Auth
- **Validación:** Zod + React Hook Form
- **Fuentes:** Inter + Playfair Display

## Configuración Inicial

### 1. Instalar Dependencias

```bash
npm install
```

### 2. Configurar Supabase

#### a) Crear proyecto en Supabase

1. Ve a [https://supabase.com](https://supabase.com) y crea una cuenta
2. Crea un nuevo proyecto
3. Guarda tu URL del proyecto y la API Key

#### b) Crear tablas en Supabase

Ejecuta los siguientes scripts SQL en el SQL Editor de Supabase:

```sql
-- Tabla de funciones de teatro
create table public.theatre_functions (
  id bigint generated by default as identity not null,
  nombre_funcion character varying not null,
  descripcion_funcion character varying null,
  valor_entrada_funcion integer null,
  imagen_funcion character varying null,
  constraint theatre_functions_pkey primary key (id)
) tablespace pg_default;

-- Tabla de usuarios/contactos
create table public.users (
  id integer generated by default as identity not null,
  name_user character varying not null,
  email_user character varying null,
  number_user integer null,
  constraint users_pkey primary key (id)
) tablespace pg_default;
```

#### c) Configurar variables de entorno

1. Copia el archivo `env.example.txt`:
   ```bash
   copy env.example.txt .env.local
   ```

2. Edita `.env.local` y reemplaza con tus credenciales de Supabase:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key-aqui
   ```

### 3. Personalizar Información del Teatro

Hay varios archivos donde debes actualizar la información del teatro (marcados con `TODO:`):

#### Header - Nombre del Teatro
📁 `components/layout/Header.tsx` (línea 18)
```typescript
const THEATER_NAME = 'Teatro Estrella'  // Cambia esto
```

#### Información del Teatro
📁 `components/home/TheaterInfo.tsx` (líneas 11-24)
```typescript
const THEATER_ADDRESS = 'Tu dirección aquí'
const THEATER_PHONE = '+54 11 1234-5678'
const THEATER_EMAIL = 'tu@email.com'
const SOCIAL_MEDIA = {
  facebook: 'https://facebook.com/tu-teatro',
  instagram: 'https://instagram.com/tu-teatro',
  twitter: 'https://twitter.com/tu-teatro',
}
const THEATER_HISTORY = 'Tu historia aquí...'
```

### 4. Agregar Funciones de Teatro

Ve a Supabase y agrega funciones de ejemplo en la tabla `theatre_functions`:

```sql
INSERT INTO theatre_functions (nombre_funcion, descripcion_funcion, valor_entrada_funcion, imagen_funcion)
VALUES 
  ('Hamlet', 'La tragedia clásica de Shakespeare', 5000, 'url-de-imagen'),
  ('La Casa de Bernarda Alba', 'Drama de Federico García Lorca', 4500, 'url-de-imagen'),
  ('Esperando a Godot', 'Obra absurdista de Beckett', 4000, 'url-de-imagen');
```

## Ejecutar en Desarrollo

```bash
npm run dev
```

Abre [http://localhost:3000](http://localhost:3000) en tu navegador.

## Construir para Producción

```bash
npm run build
npm run start
```

## Estructura del Proyecto

```
├── app/
│   ├── contacto/         # Página de contacto
│   ├── funciones/        # Listado de funciones
│   ├── login/            # Página de login
│   ├── register/         # Página de registro
│   ├── layout.tsx        # Layout principal
│   ├── page.tsx          # Página de inicio
│   └── globals.css       # Estilos globales
├── components/
│   ├── home/             # Componentes de la página de inicio
│   ├── layout/           # Header, Navbar, Search
│   └── shows/            # Componentes de funciones
├── lib/
│   ├── supabase/         # Configuración de Supabase
│   └── validations/      # Esquemas de validación Zod
├── types/
│   └── database.types.ts # Tipos de TypeScript
└── middleware.ts         # Middleware de autenticación
```

## Funcionalidades Principales

### Autenticación
- Login y registro de usuarios
- Validación de contraseña: mínimo 10 caracteres + símbolo especial
- Sesión persistente con Supabase Auth
- Protección de rutas con middleware

### Funciones
- Listado completo con tarjetas
- Ordenamiento alfabético (A-Z, Z-A)
- Ordenamiento por precio (menor a mayor, mayor a menor)
- Búsqueda en tiempo real
- Diseño responsive en grid

### Contacto
- Formulario con validación
- Campos: nombre, apellido, teléfono, email, mensaje
- Guardado automático en Supabase
- Mensajes de éxito/error

## Personalización

Todos los colores, tipografías y espaciados están definidos como variables CSS en `app/globals.css`. Puedes personalizarlos fácilmente:

```css
:root {
  --primary-600: #9333ea;  /* Color principal */
  --accent-500: #f97316;   /* Color de acento */
  /* ... más variables */
}
```

## Soporte

Si encuentras algún problema, verifica que:
1. Las credenciales de Supabase estén correctamente configuradas
2. Las tablas de la base de datos estén creadas
3. Las variables de entorno estén en el archivo `.env.local`

## Licencia

Este proyecto fue desarrollado como una solución SaaS personalizada para venta de tickets de teatro.
